"use client";

// Compatibility shim: the order UI now lives in components/order/*.
// This keeps existing imports (@/components/order-experience) working.
export { OrderExperience } from "./order/OrderExperience";
